# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:55432/ticketrush
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# JWT config
jwt.secret=DayLaChuoiBiMatCuaRiengToi2026
jwt.expiration=86400000
# OAuth2 Google Login
spring.security.oauth2.client.registration.google.client-id=1020628878774-f0v8s3446q0hu0plim61cdh5hn047pef.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
# Frontend URL to receive OAuth2 token after Google login
app.oauth2.redirect-uri=http://localhost:5173/auth
# Server port
server.port=8080
# Multipart upload limits
spring.servlet.multipart.max-file-size=${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:20MB}
spring.servlet.multipart.max-request-size=${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:25MB}
# Email (SMTP) for registration and password reset
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dangkhuat50@gmail.com
spring.mail.password=iwhwnfexafgrllst
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
# OTP/verification code settings (minutes)
app.auth.code-expiration-minutes=${APP_AUTH_CODE_EXPIRATION_MINUTES:10}
# Development fallback: set false to force real email delivery.
app.auth.dev-return-code=false
# MinIO object storage
app.minio.enabled=${APP_MINIO_ENABLED:true}
app.minio.endpoint=${APP_MINIO_ENDPOINT:http://localhost:9000}
app.minio.access-key=${APP_MINIO_ACCESS_KEY:minioadmin}
app.minio.secret-key=${APP_MINIO_SECRET_KEY:minioadmin}
app.minio.bucket=${APP_MINIO_BUCKET:ticketrush-images}
app.minio.public-base-url=${APP_MINIO_PUBLIC_BASE_URL:http://localhost:9000}
# RabbitMQ
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}
spring.rabbitmq.connection-timeout=${SPRING_RABBITMQ_CONNECTION_TIMEOUT:10000}
spring.rabbitmq.requested-heartbeat=${SPRING_RABBITMQ_REQUESTED_HEARTBEAT:30s}
# VNPay payment gateway
vnpay.tmn-code=${VNPAY_TMN_CODE:test}
vnpay.hash-secret=${VNPAY_HASH_SECRET:test}
vnpay.pay-url=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:8080/api/public/payments/vnpay/return}
vnpay.expire-minutes=${VNPAY_EXPIRE_MINUTES:15}
app.payment.result-url=${APP_PAYMENT_RESULT_URL:http://localhost:5173/payment-result}
# Auth email async queue
app.auth.email.queue-name=${APP_AUTH_EMAIL_QUEUE_NAME:ticketrush.auth.email.queue}
app.auth.email.exchange-name=${APP_AUTH_EMAIL_EXCHANGE_NAME:ticketrush.auth.email.exchange}
app.auth.email.routing-key=${APP_AUTH_EMAIL_ROUTING_KEY:ticketrush.auth.email.send}
# Redis
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
# Flash sale queue and throttle
app.flash-sale.queue-name=${APP_FLASH_SALE_QUEUE_NAME:ticketrush.flashsale.order.queue}
app.flash-sale.exchange-name=${APP_FLASH_SALE_EXCHANGE_NAME:ticketrush.flashsale.exchange}
app.flash-sale.routing-key=${APP_FLASH_SALE_ROUTING_KEY:ticketrush.flashsale.order}
app.flash-sale.consumer.max-per-second=${APP_FLASH_SALE_CONSUMER_MAX_PER_SECOND:20}
app.flash-sale.redis-hold-seconds=${APP_FLASH_SALE_REDIS_HOLD_SECONDS:600}
# Virtual queue waiting room
app.virtual-queue.enabled=${APP_VIRTUAL_QUEUE_ENABLED:true}
app.virtual-queue.batch-size=${APP_VIRTUAL_QUEUE_BATCH_SIZE:50}
app.virtual-queue.max-active-tokens=${APP_VIRTUAL_QUEUE_MAX_ACTIVE_TOKENS:200}
app.virtual-queue.admit-ttl-seconds=${APP_VIRTUAL_QUEUE_ADMIT_TTL_SECONDS:180}
app.virtual-queue.waiting-record-ttl-seconds=${APP_VIRTUAL_QUEUE_WAITING_RECORD_TTL_SECONDS:3600}
app.virtual-queue.estimated-batch-seconds=${APP_VIRTUAL_QUEUE_ESTIMATED_BATCH_SECONDS:8}
app.virtual-queue.dispatch-cooldown-ms=${APP_VIRTUAL_QUEUE_DISPATCH_COOLDOWN_MS:400}
# Event defaults
app.events.featured-limit=${APP_EVENTS_FEATURED_LIMIT:6}
app.seed.demo-event.enabled=${APP_SEED_DEMO_EVENT_ENABLED:true}
# Seat hold lifecycle
app.seats.hold-minutes=${APP_SEATS_HOLD_MINUTES:10}
app.seats.release-worker-interval-ms=${APP_SEATS_RELEASE_WORKER_INTERVAL_MS:10000}
